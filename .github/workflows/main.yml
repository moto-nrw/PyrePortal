name: PyrePortal CI
run-name: CI workflow

on:
  pull_request:
    branches:
      - main
      - development
  push:
    branches:
      - development
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  lint:
    uses: ./.github/workflows/lint.yml